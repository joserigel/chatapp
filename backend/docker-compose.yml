version: "3.8"
services:
  mongo:
    image: mongo
    restart: always
    volumes:
      - type: volume
        source: chat
        target: /data/db
    ports:
      - "27017:27017"
volumes:
  chat: 